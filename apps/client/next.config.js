/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['product-chase.s3.us-east-1.amazonaws.com','product-chase.s3.amazonaws.com'],
  },
  /* other config options here */
};

module.exports = nextConfig; 